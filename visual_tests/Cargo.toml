[package]
name = "visual_tests"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
image = "0.25"
image-compare = "0.4"
thiserror = "1.0"

[dev-dependencies]
# No dev dependencies needed - tests use the library
